['boltcutters'] = {
    label = 'Boltcutters',
    weight = 220,
    stack = true,
    close = true
},

['pcuffkey'] = {
    label = 'Police Handcuff Key',
    weight = 220,
    stack = true,
    close = true
},

['cuffkey'] = {
    label = 'Handcuff Key',
    weight = 220,
    stack = true,
    close = true
},

['cuffs'] = {
    label = 'Handcuffs',
    weight = 220,
    stack = true,
    close = true
},

['zipcuffs'] = {
    label = 'Ziptie Cuffs',
    weight = 220,
    stack = true,
    close = true
},

['ziptie'] = {
    label = 'Ziptie',
    weight = 220,
    stack = true,
    close = true
},

['pliers'] = {
    label = 'Pliers',
    weight = 220,
    stack = true,
    close = true
},

['spikes'] = {
    label = 'Spikestrip',
    weight = 220,
    stack = true,
    close = true
},

['barrier'] = {
    label = 'Barrier',
    weight = 220,
    stack = true,
    close = true
},

['cone'] = {
    label = 'Cone',
    weight = 220,
    stack = true,
    close = true
},

Item('boltcutters', function(data, slot)
    ox_inventory:useItem(data, function(data)
        if data then
        end
    end)
end)

Item('pcuffkey', function(data, slot)
    ox_inventory:useItem(data, function(data)
        if data then
        end
    end)
end)

Item('cuffkey', function(data, slot)
    ox_inventory:useItem(data, function(data)
        if data then
        end
    end)
end)

Item('cuffs', function(data, slot)
    ox_inventory:useItem(data, function(data)
        if data then
        end
    end)
end)

Item('zipcuffs', function(data, slot)
    ox_inventory:useItem(data, function(data)
        if data then
        end
    end)
end)

Item('ziptie', function(data, slot)
    ox_inventory:useItem(data, function(data)
        if data then
        end
    end)
end)

Item('pliers', function(data, slot)
    ox_inventory:useItem(data, function(data)
        if data then
        end
    end)
end)

Item('spikes', function(data, slot)
    ox_inventory:useItem(data, function(data)
        if data then
        end
    end)
end)

Item('barrier', function(data, slot)
    ox_inventory:useItem(data, function(data)
        if data then
        end
    end)
end)

Item('cone', function(data, slot)
    ox_inventory:useItem(data, function(data)
        if data then
        end
    end)
end)

